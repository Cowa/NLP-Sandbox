import maui.TopicExtractorAE
import scalaz.stream.Process
import com.github.jenshaase.uimascala.core.stream._

object Main extends App {
  /*val ae = new TopicExtractorAE()
  val p = Process("this is Richard Parker") |>
    casFromText |>
    annotate(ae)*/
}
